# FastAPI Authentication Project

This project demonstrates JWT authentication with FastAPI and SQLAlchemy.
Setup instructions:
1. Install dependencies: `pip install -r requirements.txt`
2. Run the server: `uvicorn app.main:app --reload`
